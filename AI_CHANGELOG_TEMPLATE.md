# AI 更新日志模板

## 重要说明

此模板供 AI 助手在多对话协作项目中使用，确保跨对话的工作连续性。

模板创建者：huaaaji（人类开发者，有ai协助制作）
模板创建时间2026年4月6日深夜
最新更新时间2026年4月7日夜

***

## 模板内容

```markdown
# 更新日志 (CHANGELOG)

说明和注意要点：
1.本文档记录项目的**所有重要更改**。
2.由于上下文问题，一个项目会使用多个对话完成，你作为其中一个对话，**必须写这个文档**，并且必须**标注你是哪个对话**，并且你**不能擅自猜测你是哪个对话**，如果上下文里没有明确写，必须询问人类开发者
3.**"人类开发者注释"** 表示这段是人类开发者的意见，需要认真参考
4.**不得删除**不是这个对话的任何内容，如果是之前所写的问题或下一步计划，现在已经解决或完成的，你只能在自己的部分说解决这个问题或完成这个计划，而不能删除之前的内容

---

## [YYYY-MM-DD 对话X] - 本对话的工作标题

### 新增
- 新增了哪些功能（没有则空）

### 修改
- 修改了哪些功能（没有则空）

### 修复
- 修复了哪些问题（没有则空）

### 发现的问题
- 发现的新问题（如果人类开发者标记"暂时忽略"，则后续对话不应主动实现）（没有则空）

### 待处理问题、未完成事项、目标（汇总）
| 问题 | 优先级 | 说明 |
|------|--------|------|
| 问题1 | 高/中/低 | 说明 |
（没有则空）

### 新增文档

| 文件 | 说明 |
|-----|------|
（没有则空）

### 已解决的问题（本对话解决）
- ✅ 问题1 - 解决说明（没有则空）

### 人类开发者与AI讨论的开发计划，开发功能规划
- 
（没有则空）

### 人类开发者注释
- [人类开发者的具体意见，如果有]（人类开发者的意见可以写在文档的任何位置）

### 提交记录
- 分支: xxx
- 提交: xxx
- 作者: huaaaji-bot
（或许有多次）（没有则空）

---

## 历史记录（保留之前的对话内容）
（之前的对话内容保持不变）
```

***

## 使用流程

### 1. 对话开始时

1. **读取 CHANGELOG.md** - 了解项目历史和待处理问题
2. **确认对话编号** - 如果不确定，询问人类开发者：
   ```
   您好，我想确认一下我是"对话几"？因为上下文没有明确说明，请告诉我正确的对话编号。
   ```
3. **查看待处理问题** - 选择要处理的任务

### 2. 工作过程中

1. **记录所有更改** - 包括新增、修改、修复、发现的问题
2. **更新待处理问题表** - 添加新发现的问题
3. **标记已解决问题** - 在"已解决的问题"部分记录

### 3. 对话结束时

1. **完善 CHANGELOG.md** - 确保格式正确

***

## 注意要点

注意要点：
1.文档记录项目的**所有重要更改**。
2.**"人类开发者注释"** 表示这段是人类开发者的意见，需要认真参考
3.**不得删除**不是这个对话的任何内容，如果是之前所写的问题或下一步计划，现在已经解决或完成的，你只能在自己的部分说解决这个问题或完成这个计划，而不能删除之前的内容
4.你不能擅自提交github，但是你可以提醒人类开发者提交github
5.如果人类开发者没有特殊要求，提交github时应当以用户`huaaaji-bot`提交，并且你提交的分支应当是`<你的身份>_AI`，比如如果你现在是trae（一个ai IDE），那么你提交的分支就应该是`trae_AI`，在提交的commit说明里也需要标注你的身份（比如`trae`），以及你是什么模型，比如`GLM-5`
6. **必须标注对话编号**：在标题中写明是"对话几"
7. **必须写文档**：每个对话都要在 CHANGELOG.md 中记录工作内容
8. **不得删除之前的内容**：只能添加新内容，不能删除之前对话的内容
9. **不得擅自猜测对话编号**：如果上下文没有明确说明，必须询问人类开发者
10. **人类开发者注释**：必须认真参考

***

## 示例

### 更新记录示例（只包含部分内容）

```markdown
## [2026-04-06 对话三] - HueSatMap 应用实现与色彩空间优化

### 新增

#### HueSatMap 模块 (huesat_map/)
- **实现 `applyHueSatMap()` 函数**: 将 HueSatMap 应用到图像
  - 高精度 RGB → HSV 转换（手动实现，避免 OpenCV 16位精度损失）
  - 高精度 HSV → RGB 转换
  - 三线性插值在 3D 查找表中获取变换参数
  - 全程 CV_32FC3 浮点运算，无精度损失
- **新增私有函数**:
  - `rgbToHsv()`: RGB → HSV 转换
  - `hsvToRgb()`: HSV → RGB 转换
  - `trilinearInterpolate()`: 三线性插值

### 修改

#### 色彩空间转换模块 (color_transform/)
- **重命名函数**，提高可读性：
  - `d50_2_d65(LibRaw& source)` → `XYZ_D50_to_D65()`
  - `readccmrgb(LibRaw& source)` → `XYZ_D65_to_sRGB()`
- **移除无用参数**: 上述函数不再需要 LibRaw 参数

### 发现的问题

#### sRGB Gamma 不正确
- 当前使用纯 2.2 gamma: `pow(x, 1/2.2)`
- 正确的 sRGB gamma 应为分段函数

人类开发者注释：暂时忽略

### 待处理问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| sRGB Gamma 分段函数 | 中 | 影响高光/暗部色调准确性 |
| `WB_culculate()` 实现 | 高 | As Shot Neutral → 色温 + Tint |

### 已解决的问题
- ✅ **HueSatMap 应用函数未实现** - 已在本对话实现 `applyHueSatMap()` 函数

### 提交记录
- 分支: `trae-AI`
- 提交: `caeefd1`
- 作者: huaaaji-bot
```

### 提交规范说明

#### 情况1：人类开发者有特殊要求
当人类开发者明确指定提交方式时，按照人类开发者的要求执行。

#### 情况2：人类开发者没有特殊要求
当人类开发者没有明确指定提交方式时，AI 必须按照以下规范执行：
- 用户：`huaaaji-bot`
- 分支：`<AI身份>_AI`（如 `trae_AI`）
- Commit 格式：`[AI身份 - 模型名称] 说明`

***

## 常见问题

### Q: 发现之前对话的问题已经解决了，怎么办？

A: 不要删除之前对话的内容。在自己的对话部分添加"已解决的问题"条目。

### Q: 人类开发者标记"暂时忽略"的问题，我可以实现吗？

A: 不可以。必须等待人类开发者明确要求实现。

### Q: 不确定自己是哪个对话怎么办？

A: 必须询问人类开发者，不能擅自猜测。
